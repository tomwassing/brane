query GetPackages {
    packages {
        created,
        description,
        detached,
        digest,
        functionsAsJson,
        id,
        kind,
        name,
        owners,
        typesAsJson,
        version
    }
}